<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.epicodus.loglegal.LogbookActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:showDividers="end"
        android:orientation="vertical"
        android:divider="@drawable/divider_drawable"
        android:id="@+id/linearLayout">

        <ScrollView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/scrollView">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/incident"
                android:text="New Text" />

        </ScrollView>
    </LinearLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:text="@string/find_legal_search_title"
        android:id="@+id/findLegalSearchTitle"
        android:layout_above="@+id/zipcodeInput"
        android:layout_centerHorizontal="true"
        android:textColor="@color/colorPrimaryText"
        android:textStyle="bold"
        android:textSize="20sp" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:ems="10"
        android:padding="5dp"
        android:background="@color/colorPrimaryLight"
        android:textColor="@color/colorSecondaryText"
        android:hint="@string/find_legal_input_hint"
        android:id="@+id/zipcodeInput"
        android:layout_above="@+id/findLegalButton"
        android:layout_marginBottom="10dp"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:drawableLeft="@drawable/legal_white"
        android:drawableStart="@drawable/legal_white"
        android:id="@+id/findLegalButton"
        android:background="@color/colorPrimaryDark"
        android:singleLine="true"
        android:paddingStart="20dp"
        android:paddingLeft="20dp"
        android:paddingEnd="20dp"
        android:paddingRight="20dp"
        android:textSize="20sp"
        android:contentDescription="@string/find_legal_activity_button_description"
        android:text="@string/find_legal_activity_button_description"
        android:textColor="@color/colorIcons"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

</RelativeLayout>
